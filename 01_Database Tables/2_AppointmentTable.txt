create table appointment (
	apt_code	varchar(5)	not null,
	apt_date	date,
	apt_starttime	varchar(8),
	apt_endtime	varchar(8),
	cust_id		varchar(5)	not null,
primary key(apt_code),
foreign key(cust_id) references customer(cust_id)
);
